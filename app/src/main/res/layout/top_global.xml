<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_gradient">

    <!-- Top 50 Cover -->

    <!-- Description -->
    <ImageView
        android:id="@+id/top_50_cover"
        android:layout_width="160dp"
        android:layout_height="160dp"
        android:layout_marginTop="76dp"
        android:scaleType="fitXY"
        android:src="@drawable/chart_background_gradient_blue"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.498"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_margin="100dp"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/playlist_description"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Your daily update of the most played tracks right now - Global."
        android:textColor="#AAAAAA"
        android:textSize="12sp"
        android:gravity="center"
        app:layout_constraintTop_toBottomOf="@id/top_50_cover"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="12dp"
        android:layout_marginHorizontal="24dp"/>

    <!-- Info -->
    <TextView
        android:id="@+id/playlist_info"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Purrtify  •  Apr 2025  •  2h 55min"
        android:textColor="#CCCCCC"
        android:textSize="12sp"
        app:layout_constraintTop_toBottomOf="@id/playlist_description"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="8dp"/>

    <!-- Play Button -->
    <ImageView
        android:id="@+id/play_button"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:src="@drawable/ic_play"
        android:background="@drawable/circle_green"
        android:padding="12dp"
        app:layout_constraintTop_toBottomOf="@id/playlist_info"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="16dp"/>

    <!-- RecyclerView Track List -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/track_list"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:clipToPadding="false"
        android:paddingHorizontal="16dp"
        android:paddingBottom="80dp"
        app:layout_constraintTop_toBottomOf="@+id/play_button"
        app:layout_constraintBottom_toTopOf="@+id/bottom_navigation"/>

    <!-- Bottom Navigation -->
    <LinearLayout
        android:id="@+id/bottom_navigation"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:background="#121212"
        android:paddingVertical="8dp"
        android:orientation="horizontal"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- Home, Library, Profile Icons -->
        <!-- ...same as your previous layout, omitted for brevity... -->

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
